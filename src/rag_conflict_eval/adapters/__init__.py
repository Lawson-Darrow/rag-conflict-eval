"""Framework adapters. Import the submodule you need, e.g.::

    from rag_conflict_eval.adapters.deepeval import BehaviorAdherenceMetric

DeepEval is an optional dependency (``pip install rag-conflict-eval[deepeval]``).
"""
